name: flutter_video_app
description: a play anime app
publish_to: "none"

version: 0.12.2
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 本地化
  flutter_localizations:
    sdk: flutter
  intl:

  cupertino_icons: ^1.0.0
  rxdart: ^0.24.1
  built_value:
  built_collection:
  url_launcher: ^5.7.2
  mobx: ^1.2.1+2
  flutter_mobx: ^1.1.0+2
  html: ^0.14.0+4
  webview_flutter:
    git:
      url: https://github.com/januwA/webview_flutter-1.0.3
      ref: main

  sqflite: ^1.3.1+1 #sqlite
  sqflite_common_ffi:
  path:
  dart_printf: ^0.4.0

  package_info: '>=0.4.3 <2.0.0'
  flutter_slidable: ^0.5.4 #可滑动列表项

  # 底部泡泡栏
  bubble_bottom_bar: ^1.2.0

  video_box: ^0.15.4
  flutter_ajanuw_router: ^0.3.5
  ajanuw_http: ^0.5.1
  flutter_github_releases_service: ^0.5.1 # apk升级策略
  breakpoints: ^0.2.0
  flutter_imagenetwork: ^0.6.1
  get_it: ^5.0.1 # 动态服务定位器
  toast:
  flutter_screen:
    git:
      url: https://github.com/januwA/flutter_screen
      ref: master
  flutter_ajanuw_android_pip: ^0.1.2
  shared_preferences: '>=0.5.12+2 <2.0.0' # key-value 储存数据

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  built_value_generator:
  mobx_codegen:
  moor_generator:
dependency_overrides:
  analyzer: 0.41.1

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
