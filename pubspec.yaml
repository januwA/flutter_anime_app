name: anime_app
description: a play anime app
publish_to: "none"

version: 0.13.4
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 本地化
  flutter_localizations:
    sdk: flutter
  intl:

  cupertino_icons: ^1.0.2
  rxdart: ^0.26.0
  built_value:
  built_collection:
  url_launcher: ^6.0.2
  mobx: ^1.2.1+2
  flutter_mobx: ^1.1.0+2
  html: ^0.15.0
  webview_flutter:
    git:
      url: git@github.com:januwA/webview_flutter-2.0.2.git
      ref: main

  sqflite: ^2.0.0+2 #sqlite
  sqflite_common_ffi:
  path:
  dart_printf: ^0.5.0

  package_info: ^2.0.0
  flutter_slidable: ^0.5.7 #可滑动列表项

  # 底部泡泡栏
  bubble_bottom_bar: ^1.2.0

  video_box: ^0.15.6
  flutter_ajanuw_router: ^0.3.5
  ajanuw_http: ^0.6.0
  flutter_github_releases_service: ^0.5.2 # apk升级策略
  breakpoints: ^0.2.0
  flutter_imagenetwork: ^0.6.1
  get_it: ^6.0.0 # 动态服务定位器
  toast:
  flutter_screen:
    git:
      url: git@github.com:januwA/flutter_screen.git
      ref: master
  flutter_ajanuw_android_pip: ^0.1.2
  shared_preferences: ^2.0.4 # key-value 储存数据

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  built_value_generator:
  mobx_codegen:
  moor_generator:

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
